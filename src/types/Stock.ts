export interface Stock {
    symbol: string;
    price: number;
    timestamp: string;
  }

// export interface Stock {
//   symbol: string;
//   currentPrice: number;
//   companyName?: string;  // optional if you're not using it now
// }
